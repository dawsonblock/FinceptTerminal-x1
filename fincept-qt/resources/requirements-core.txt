# ============================================================
# Fincept Terminal — CORE runtime dependencies
# These packages are required for the app to launch and for
# the main supported workflows (market data, charting, news,
# broker connectivity, basic AI chat).
#
# Install: pip install -r requirements-core.txt
# ============================================================

# Numeric + data
numpy>=2.2.3,<3.0
pandas>=2.3.3,<3.0

# HTTP / async networking
requests==2.32.5
aiohttp==3.13.2
websockets==15.0.1

# Market data (essential sources)
yfinance==0.2.66
ccxt>=4.5.44
akshare>=1.14.0

# News / feeds
feedparser==6.0.12

# HTML parsing
beautifulsoup4==4.14.2

# Serialisation / data formats
protobuf>=3.20.2,<6.0.0
pyarrow>=16.0.0

# LLM gateway (AI chat)
openai>=2.8.1,<3.0
anthropic>=0.71.0,<1.0

# AI search tools (used by Agno free tools)
ddgs>=6.0.0
wikipedia>=1.4.0

# Agno orchestration framework
agno>=1.0.0

# Utilities
loguru>=0.6.0
pydantic-settings>=2.0.0
python-dateutil==2.9.0.post0
pytz==2025.2
certifi==2025.11.12
idna==3.11
urllib3==2.5.0
charset-normalizer==3.4.4
typing_extensions==4.15.0
six==1.17.0
tzdata==2025.2
